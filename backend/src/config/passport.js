// backend/src/config/passport.js
// Hiện hệ thống xác thực bằng JWT + middleware `protect`,
// nên Passport chưa cần thiết. File này để phòng sau này mở rộng.

export const initPassport = () => {
  // TODO: nếu muốn dùng passport-jwt thì cấu hình tại đây.
  // Tạm thời để trống cho phù hợp với kiến trúc đã chọn.
};
